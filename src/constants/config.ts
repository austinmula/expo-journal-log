// Application configuration constants

export const config = {
  // Auto-save delay in milliseconds
  autoSaveDelay: 2000,

  // Debounce delay for search in milliseconds
  searchDebounceDelay: 300,

  // Number of days before permanently deleting trashed entries
  trashRetentionDays: 30,

  // Maximum preview length for entry cards
  previewMaxLength: 150,

  // Maximum number of tags to display on entry cards
  maxTagsOnCard: 3,

  // Minimum search query length
  minSearchLength: 2,

  // Default page size for entry lists
  defaultPageSize: 20,
};
